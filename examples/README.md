# Examples

This directory contains examples used for documentation and as reference configs.

* **provider/provider.tf** — provider configuration example
* **data-sources/`full data source name`/data-source.tf** — data source examples
* **resources/`full resource name`/resource.tf** — resource examples with `import.sh`
* **workflows/main.tf** — full-stack reference deployment (Flask + Postgres)
